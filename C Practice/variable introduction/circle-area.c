
#include<stdio.h>

int main(){
    /*
    printf("Enter radius: ");

    double radius;
    scanf("%lf",&radius);
    const double PI = 3.1416;

    double area = PI*radius*radius;
    double perimeter = 2 * PI * radius;

    printf("Area of the circle is = %.4lf\n",area);
    printf("Perimeter is = %.2lf\n", perimeter);

    int alpha = 30;
scanf("%d", &alpha);
alpha = alpha * 2;
alpha--;
printf("MCQ ans = %d",alpha);

int a, b;
	scanf("%d %d", &b, &a);
	a /= b;
	printf("MCQ two = %d %d\n", a, b);
	return 0;
*/
int beta = -10.5;
printf("%d\n",beta);
int a = 5;
int b = 2;
double q = (double)a/(double)b;
printf("Siam's value => %d / %d = %lf",a,b,q);

 return 0;
}
